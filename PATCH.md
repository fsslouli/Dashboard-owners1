# هذا التحديث — وش سوّيناه ووش باقي عليك

كل تعديلات الكود مطبّقة فعليًا داخل `Dashboard.jsx` بهذا الملف — ارفع المجلد كامل
لـ GitHub زي ما هو، وVercel يبني وينشر تلقائي. ما فيه أي خطوة نسخ-لصق باقية بالكود.

## اللي انضاف

- **`gallery-kit.jsx`** — معرض الموقع (مواضيع فيها صور ومقاطع، رفع مباشر أو رابط
  خارجي، ترتيب، إخفاء/إظهار، حذف) + تبويب إدارة "معرض الموقع" + عرض عام بتبويب
  جديد "الصور والمقاطع".
- **`nav-labels-kit.jsx`** — تبويب إدارة "تسمية الأقسام" يعدّل أسماء كل تبويبات
  الموقع العام ولوحة الإدارة لحظيًا.
- **`translate-kit.js`** + **`supabase/functions/translate/index.ts`** — ترجمة
  عربي→إنجليزي تلقائية بالخلفية، شغّالة الحين بعناوين المعرض وحقول "تسمية
  الأقسام" وعنوان مقاطع النماذج.

## خطوتين برّا الكود، لازم تسويهم إنت بعد الرفع (ما ينسويهم Git ولا Vercel)

### 1) قاعدة البيانات — شغّل مرة وحدة بـ SQL Editor بمشروع Supabase

بالترتيب: `migration-gallery.sql` ثم `migration-nav-labels.sql`.

### 2) الترجمة التلقائية — نشر الدالة ومفتاحها

```bash
supabase functions deploy translate --project-ref codnqkeycfhznzbqlpds
supabase secrets set GOOGLE_TRANSLATE_API_KEY=مفتاحك --project-ref codnqkeycfhznzbqlpds
```

المفتاح من Google Cloud Console → فعّل "Cloud Translation API" → أنشئ API key
(فيه حصة مجانية شهرية تكفي هذا الاستخدام براحة). لين ما تسوي هالخطوة، الموقع
يشتغل عادي بدون ما يتعطّل — بس الإنجليزي التلقائي ما ينحط، وتقدر تكتبه يدوي زي
العادة.

## باقي — لو تبي تكمّلها لاحقًا

ترجمة تلقائية لملاحظات وردود الاستفسارات (note/reply) — ودّي أراجعها بعناية
أول لأن الحقلين يتكرران بأكثر من مكان بملف كبير. قلّي "كمّل الاستفسارات" وقت ما تبي.
