# هذا التحديث (٢.١٠.٠) — وش سوّيناه ووش باقي عليك

كل التعديلات مطبّقة داخل الملفات — ارفع المجلد كامل لـ GitHub زي ما هو، وVercel يبني وينشر تلقائي.
بعد النشر يبقى الموقع على تصميمه الحالي، و«بنّاء» يظهر كخيار ثالث بلوحة الإدارة.

## اللي انضاف

- **`design-bannaa.jsx`** — التصميم الثالث «بنّاء»: جدار الملاحظات (كل استفسار لبنة بلون قراره،
  والأحدث بالأعلى، والمفرّغة ما زالت مفتوحة) وأنماط التصميم كاملة.
- لوحة الإدارة ← «مظهر الموقع»: ثلاث تصاميم بدل اثنين، مع معاينة مصغّرة لـ«بنّاء».
- التفاصيل كاملة بأول قسم في `README.md`.

## خطوة وحدة برّا الكود — قبل ما تعتمد «بنّاء»

شغّل **`migration-design-bannaa.sql`** مرة وحدة بـ SQL Editor بمشروع Supabase.
قاعدة البيانات حاليًا تقبل «الكلاسيكي» و«نوفا» بس — الملف يوسّع القيد فقط، وما يغيّر شي بالموقع.

بعدها: `#admin` ← «مظهر الموقع» ← «بنّاء» ← «اعتماد».

## باقي من التحديث السابق (لو ما سويته للحين)

نشر دالة الترجمة التلقائية ومفتاحها:

```bash
supabase functions deploy translate --project-ref codnqkeycfhznzbqlpds
supabase secrets set GOOGLE_TRANSLATE_API_KEY=مفتاحك --project-ref codnqkeycfhznzbqlpds
```

وترجمة ملاحظات وردود الاستفسارات تلقائيًا — قلّي «كمّل الاستفسارات» وقت ما تبي.
